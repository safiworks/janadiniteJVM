[package]
name = "janadinite-parse"
version = "0.1.0"
edition = "2024"
authors = ["safiworks safiworks0@gmail.com"]
license = "MIT"
description = "A no-std rust JVM decoding and encoding library"

[dependencies]
bitflags = "2.13.1"
simd_cesu8 = { version = "1.2.0", default-features = false }
macros = { path = "../macros" }

[features]
std = []
default = ["std"]
